# تشغيل المشروع

مدير جلسات واتساب يُتحكم به عبر بوت تليجرام. مبني على `grammy` + `@whiskeysockets/baileys`.

---

## المتطلبات

- **Node.js 20 أو أحدث** (مطلوب من Baileys)
- npm

---

## التثبيت والتشغيل

### 1. ثبّت التبعيات

```bash
npm install
```

### 2. جهّز الإعدادات

```bash
cp .env.example .env
```

ثم افتح `.env` واملأ القيمين المطلوبتين:

```env
TELEGRAM_BOT_TOKEN=123456:AAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OWNER_ID=6707747395
```

- **التوكن** من [@BotFather](https://t.me/BotFather) (`/newbot`)
- **المعرف** الرقمي حقك في تليجرام — مو اسم المستخدم. تحصل عليه من `@userinfobot`

### 3. تحقق من الإعدادات

```bash
npm run config:check
```

### 4. افحص اتصال واتساب

```bash
npm run doctor
```

هذه الخطوة **مهمة قبل أول تشغيل** — تخبرك بالسبب الدقيق لو فشل الربط بدل رسائل غامضة.

```bash
npm run doctor -- --pair 628123456789   # مع طلب كود ربط
npm run doctor -- --keep                # لا يحذف مجلد الفحص المؤقت
```

### 5. شغّل

```bash
npm start
```

---

## الأوامر المتاحة للتحقق

| الأمر | الوظيفة |
|---|---|
| `npm run config:check` | يتحقق من `.env` ويقولك الناقص |
| `npm run doctor` | يشخّص اتصال واتساب |
| `npm test` | 37 اختبارًا آليًا (تخزين، أزرار، لوحة التحكم) |
| `npm start` | تشغيل البوت |

---

## 🎛 لوحة التحكم

أرسل `/panel` للبوت في الخاص (المالك والراسيلرز فقط).

| القسم | ما يوفّره |
|---|---|
| 🏠 الرئيسية | نظرة عامة: الجلسات، المستخدمون، الوضع الحر، الكولدون، الذاكرة، مدة التشغيل |
| 📱 الجلسات | عرض كل جلسة وحالتها، قطع الاتصال، إعادة الوصل، حذف جلسة، حذف الكل (بتأكيد) |
| 👥 المستخدمون | الوصول / الراسيلرز / المحظورون / القائمة البيضاء — مع ترقيم الصفحات وإضافة بالرسائل |
| ⚙️ الإعدادات | تبديل الوضع الحر ووضع الصيانة بضغطة زر |
| ⏱️ الكولدون | تفعيل/تعطيل، إعادة تعيين الكل، عرض الحالة والإحصائيات |
| 📊 الإحصائيات | أرقام تفصيلية عن الجلسات والكولدونات والنظام |
| 🔧 الصيانة | تنظيف الكاش، إعادة تشغيل آمنة (تغلق الجلسات أولًا) |

**ملاحظات تقنية:**
- كل الأزرار تستخدم النطاق `ap:` فلا تتعارض مع أزرار البوت الأصلية
- كل الأفعال الخطيرة (حذف، إعادة تشغيل) تطلب **تأكيدًا** أولًا
- اللوحة **لا تحتوي أي زر يشغّل أوامر الكراش/السبام** — هي إدارة فقط

---

## مستويات اللوق

في `.env`:

```env
LOG_LEVEL=info      # افتراضي
LOG_LEVEL=debug     # عند تشخيص مشاكل الاتصال
LOG_LEVEL=silent    # إسكات تام
```

> القديم كان `silent` دائمًا ومكتوب حرفيًا في الكود — كان يخفي سبب فشل الاتصال تمامًا.

---

## بنية المشروع

```
├── raju.js                  البوت الرئيسي
├── config.js                الإعدادات (تقرأ من .env — لا أسرار في الكود)
├── lib/
│   ├── store.js             طبقة تخزين JSON: مسارات مطلقة + كتابة ذرّية + كاش
│   ├── keyboards.js         مكتبة لوحات الأزرار (namespace ap:)
│   └── adminPanel.js        لوحة التحكم التفاعلية
├── controlSystem/
│   ├── control.js           الصلاحيات (المالك/الراسيلر/الوصول/الحظر)
│   ├── cooldown.js          كولدون لكل أمر
│   └── sumemek.js           كولدون متقدم (لكل مستخدم + أمر)
├── database/                إعدادات وقت التشغيل
├── storage/                 قوائم الوصول والحظر
├── session/                 جلسات واتساب (مُستثنى من git)
└── scripts/
    └── wa-doctor.js         أداة تشخيص الاتصال
```

---

## ملاحظات أمنية مهمة

1. **لا ترفع ملف `.env` أبدًا** — مستثنى في `.gitignore`
2. **لا ترفع مجلد `session/`** — فيه مفاتيح تشفير واتساب
3. لو انكشف التوكن في أي وقت: `@BotFather` → `/revoke`
4. الأوامر `/settoken` و`/setconfig` **مُعطّلة عمدًا** — كانت تعدّل الكود المصدري أثناء التشغيل

---

## قراءة إضافية

`REPORT.md` فيه القائمة الكاملة لكل مشكلة مكتشفة وحالتها.
